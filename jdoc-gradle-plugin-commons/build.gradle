plugins {
   id "java-library-conventions"
   id "dev.gradleplugins.java-gradle-plugin"
}

description = "jdoc-test gradle plugin common components"

dependencies {
    api project(":jdoc-core")

    compileOnly gradleApi("5.4")

    testImplementation gradleTestKit()
}